package com.claim_search.controller;

import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

@RestController
@RequestMapping("/") // This matches your context path
public class ClaimController {
    
    @GetMapping("/")
    public String test() {
        return "Spring Boot is working! Database connected!";
    }
    
    @GetMapping("/")
    public String healthCheck() {
        return "Application is healthy and connected to PostgreSQL!";
    }
    
    @GetMapping("/")
    public String home() {
        return "Welcome to Claims Project API!";
    }
}